# CLAUDE.md — concur-config-diver-support

Docs/artifact workspace for Concur Config Diver support. **No build, test, or lint — do not scaffold one.**

## Start here
Read `docs/WHERE-WE-LEFT-OFF.md` at the top of every session. Update it before ending one.

## Rules
- New loose files get a `YYYY-MM-DD_` prefix. Use the session's current date.
- `docs/` = durable reference. `notes/` = dated working notes. `artifacts/` = deliverables.
- Concur product facts: check `PROJECTS/concur-corpus` (Docs Genie, Professional Edition only) before answering from memory.
- Don't duplicate content that already lives in the Concur corpus — link to it.
